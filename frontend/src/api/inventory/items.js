import axios from 'axios';
const BASE_URL = "http://localhost:3000";

// the create items would create a items and store it to the database.

export const createItems = async (data) => {

};

// it would fetch all the items from the database and then we have to provide the data to medicines,surgical equipment and other categories accordingly. we have to only fetch once and show the data.

export const getAllItems = async () => {

};


